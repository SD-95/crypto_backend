services:
  - type: web
    name: crypto_backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    region: singapore
    plan: free
    branch: main
    autoDeploy: true